import React from 'react';

const GalleryFooter = ({ imagesCount, onClearGallery }) => {
  return (
    <>
      {/* Footer content removed as requested */}
    </>
  );
};

export default GalleryFooter; 